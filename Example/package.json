{
  "name": "Example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "react-native start",
    "test": "jest",
    "lint": "eslint .",
    "cleanYarn": "rm -rf node_modules/react-native-admob;rm -f yarn.lock",
    "updateIos": "cd ios;rm -f Podfile.lock;pod install --repo-update;cd ..",
    "iosInstall": "yarn cleanYarn;yarn install;yarn updateIos",
    "androidInstall": "yarn cleanYarn;yarn install"
  },
  "dependencies": {
    "react": "17.0.2",
    "react-native": "0.65.1",
    "react-native-admob": "file:.."
  },
  "devDependencies": {
    "@babel/core": "^7.6.0",
    "@babel/runtime": "^7.6.0",
    "@react-native-community/eslint-config": "^0.0.5",
    "babel-jest": "^24.9.0",
    "escape-string-regexp": "^2.0.0",
    "eslint": "^6.4.0",
    "jest": "^24.9.0",
    "metro-react-native-babel-preset": "^0.66.0",
    "react-native-codegen": "^0.0.7",
    "react-test-renderer": "17.0.2"
  },
  "jest": {
    "preset": "react-native"
  }
}
